services:
- type: web
  name: express
  env: node
  plan: starter
  buildCommand: yarn --frozen-lockfile install
  startCommand: node app.js
  disk:
    name: My Disk
    mountPath: /var/data
    sizeGB: 1
